# 族谱协议 设计方案 / Family Tree Protocol

**版本**: v0.1-draft　**状态**: 草案，未冻结　**日期**: 2026-09-02
**许可**: CC0 1.0（公有领域，见 LICENSE-SPEC）

> **本文档是本项目最重要的产物。** 代码会被重写、语言会过时、维护者会死亡，
> 但只要这份文档还能被读懂并独立实现，几十年后的人就能读出今天写入的账本。
> 因此本文档的要求是：**脱离本仓库的任何代码，仅凭本文档即可写出可互操作的实现。**

标记约定：
- `[待定]` — 需要拍板的开放问题，见 §12
- **MUST / MUST NOT / SHOULD / MAY** — 依 RFC 2119 解释

---

## 0. 命名待定事项（冻结前必须解决）

工作名为 `familytree`。**协议名会被硬编码进域分隔字符串**（§3.5）与频道派生
（§9.1），一旦有真实数据写入就无法更改，否则所有历史签名失效。

**因此命名 MUST 在第一个正式版本发布前确定。** 本文档中所有出现
`familytree/...` 的常量都是占位符。`[待定 T1]`

---

## 1. 目标与非目标

### 1.1 目标

| # | 目标 | 含义 |
|---|------|------|
| G1 | **无管理员** | 协议中不存在任何拥有特权的角色。人都会死，特权角色必然断裂。 |
| G2 | **自主持有** | 每个成员持有自己数据的完整副本。离开家庭、脱离服务后仍可继续使用。 |
| G3 | **篡改可检测** | 任何记录都能证明作者身份；任何改动都会破坏哈希链而暴露。 |
| G4 | **离线可用** | 不依赖任何服务器存在。网络仅用于同步，不用于功能。 |
| G5 | **跨代存续** | 设计时间尺度为百年量级，而非产品生命周期。 |
| G6 | **抗滥用** | 靠数据模型的形状约束，而非事后监控——监控会摧毁 G2。 |

### 1.2 非目标（刻意不做）

这些不是"以后再做"，而是**明确不做**。其中多数同时是 G6 的结构性保证，
实现者 MUST NOT 自行添加。

| # | 非目标 | 原因 |
|---|--------|------|
| N1 | 全球共识 / 区块链 | 族谱没有双花问题。冲突的声明可以共存，无需全序。 |
| N2 | 全局删除 | 分布式系统中做不到。声称能做到即为欺骗用户。 |
| N3 | 全局搜索 / 陌生人可达 | 唯一能把本协议变成分发平台的功能。 |
| N4 | 自由文本消息通道 | 一旦存在任意消息通道，所有形状约束失效。 |
| N5 | 通用文件存储 | 媒体 MUST 挂载于具体人物与日期，不存在游离 blob。 |
| N6 | 匿名性 | 身份长期且锚定于亲缘图。这是特性，不是缺陷。 |

---

## 2. 核心模型

### 2.1 一切都是声明（claim），不是事实

系统中不存在"张三的父亲是张大山"这样的事实，只存在
**"公钥 A 于某时声称：张三的父亲是张大山"**。

真实族谱本就充满互相打架的记载。系统 MUST 保留并展示冲突（§7.3），
MUST NOT 假装只有一个真相。

### 2.2 只追加，不修改

**协议中不存在"修改"操作，只有"追加声明"。**

这是整个系统的地基，也是抗夺权的核心（§6.2）。本人生前签署的内容，
后代永远只能补充，不能覆盖：

```
爷爷生前亲笔：  "1962年我在鞍山钢厂当学徒"      [本人签署 · 1998]
爸爸后来补充：  "1962-1965 鞍山钢铁厂，三级钳工"  [由子代补充 · 2026]
```

两条都存在、都展示、都标注作者与时间。展示层按 §7.2 排序。

### 2.3 身份 = 密钥对

一个身份由两个密钥对组成（§3.1）。不存在账号、密码、注册。
身份与人物节点通过 `identity.bind` 绑定（§5.1）。

### 2.4 节点 = 一份副本

一个"节点"是一份本地的事件集合 + 媒体存储。节点之间地位完全平等。
没有任何节点拥有完整族谱——**每个人看到的树是以自己为中心的**：
自己 + 祖先 + 子孙 + 拉得到的旁支。这是预期行为，不是缺陷。

---

## 3. 密码学原语

### 3.1 算法选型

| 用途 | 算法 | 规范 |
|------|------|------|
| 签名 | Ed25519 | RFC 8032 |
| 密钥协商 | X25519 | RFC 7748 |
| 哈希 | SHA-256 | FIPS 180-4 |
| KDF / MAC | HMAC-SHA256 | RFC 2104 |
| 对称加密 | XChaCha20-Poly1305 | draft-irtf-cfrg-xchacha |

一个身份持有**两个独立密钥对**：签名用 Ed25519，加密用 X25519。

> **实现者注意**：Ed25519 密钥在数学上可以转换为 X25519 使用，但该转换
> 有微妙的陷阱（符号位、小子群、跨用途签名预言机）。本协议 MUST 使用
> 两个独立生成的密钥对，MUST NOT 做转换。

### 3.2 规范化序列化（Canonical Serialization）

**这是跨实现最容易出 bug 的地方。** 两个实现只要有一个字节的差异，
签名就会在一端通过、在另一端失败，而且极难排查。

序列化 MUST 遵循 **RFC 8785 (JCS)**，并附加以下额外约束：

1. **禁止浮点数。** 所有数值 MUST 为整数。（浮点数的规范化表示是 JSON
   跨语言互操作的头号灾难源。日期用 Unix 秒，坐标用微度整数。）
2. **字符串 MUST 为 NFC 规范化的 UTF-8。**（中日韩人名、带音调的拉丁字母、
   谚文都存在多种 Unicode 表示，不规范化则同一个名字会产生不同哈希。）
3. **禁止 `null`。** 缺省字段 MUST 直接省略，MUST NOT 显式写 null。
4. 对象键按 UTF-16 码元序排序（JCS 已规定，此处强调）。
5. 未知字段 MUST 保留并参与签名验证（前向兼容）。

### 3.3 事件 ID 与签名

设 `E` 为事件信封（§4.1，**不含** `id` 与 `sig` 字段）：

```
signing_input = "familytree/event/v1" || 0x00 || JCS(E)

id  = hex( SHA-256( signing_input ) )          // 64 个小写十六进制字符
sig = Ed25519_Sign( sk_sign, signing_input )    // base64url，无填充
```

`id` 与 `sig` 覆盖同一段字节，因此二者绑定。Ed25519 是确定性签名
（RFC 8032），同一密钥对同一消息 MUST 产生相同签名。

### 3.4 编码约定

- 二进制字段（公钥、签名）：**base64url 无填充**（RFC 4648 §5）
- 哈希与 ID：**小写十六进制**
- 时间戳：整数，Unix 秒，UTC

### 3.5 域分隔

所有哈希、签名、密钥派生 MUST 带域分隔前缀，格式为
`"familytree/<用途>/v<版本>"`，前缀与内容之间以单字节 `0x00` 隔开。

已分配的域：

| 域字符串 | 用途 |
|----------|------|
| `familytree/event/v1` | 事件签名与 ID |
| `familytree/channel/v1` | 同步频道派生（§9.1） |
| `familytree/envelope/v1` | relay 信封加密（§9.2） |
| `familytree/media/v1` | 媒体内容寻址（§8.3） |

---

## 4. 事件

### 4.1 信封格式

```json
{
  "v": 1,
  "type": "person.assert",
  "author": "<Ed25519 公钥, base64url>",
  "seq": 42,
  "prev": "<同一作者上一条事件的 id>",
  "ts": 1756800000,
  "body": { }
}
```

存储与传输时附加 `id` 与 `sig`（二者可由信封重新计算/验证，不参与签名）。

| 字段 | 类型 | 说明 |
|------|------|------|
| `v` | int | 协议版本，当前为 1 |
| `type` | string | 事件类型，见 §5 |
| `author` | string | 签名公钥 |
| `seq` | int | 该作者的单调递增序号，从 0 开始 |
| `prev` | string | 该作者上一条事件的 id；`seq == 0` 时省略 |
| `ts` | int | 作者自称的时间，**不可信**，见 §4.3 |
| `body` | object | 类型相关载荷 |

### 4.2 每作者哈希链

`prev` 指向**同一作者**的上一条事件，形成每作者独立的链（而非全局 DAG）。
这样做的收益：

- **截断可检测**：删掉中间任何一条，后续 `prev` 全部对不上。
- **分叉即证据**：若出现同一 `author` 下 `seq` 相同但 `id` 不同的两条事件，
  该作者存在**等价欺骗（equivocation）**。这是其作恶的密码学证明，
  实现 MUST 保留两条并向用户明确告警。

不需要全局排序，因为不存在双花问题（N1）。

### 4.3 时间戳不可信

`ts` 由作者自行填写，可以任意伪造。

**任何安全判定 MUST NOT 依赖 `ts`。** 它仅用于展示与排序提示。
需要时序保证的场合使用哈希链（`seq`）或本地接收时间（§6.2）。

### 4.4 存储规则

节点 MUST 保存所有**签名有效**的事件，即使其权限判定当前为无效（§6.5）。
签名无效的事件 MAY 直接丢弃。

原因：权限判定会随着新事件到达而改变。今天判定越权的事件，
明天收到一条 `relation.parent` 后可能就合法了。

---

## 5. 事件类型

### 5.1 身份类

| 类型 | body | 说明 |
|------|------|------|
| `identity.create` | `{sign_pk, enc_pk, label?}` | 声明一个新身份 |
| `identity.bind` | `{person, sign_pk}` | 将人物节点绑定到身份 |
| `identity.rotate` | `{new_sign_pk, new_enc_pk}` | 密钥轮换，由旧密钥签署 |
| `identity.guardians` | `{guardians:[pk], threshold:k}` | 设置社交恢复守护人 |
| `identity.recover` | `{person, new_sign_pk, new_enc_pk, endorsements:[...]}` | 守护人 k-of-n 签发新密钥 |
| `identity.succession` | `{heirs:[pk], threshold:k}` | 遗嘱式继承：死后由谁管理本人节点 |

**`identity.bind` 的权限规则（关键，这是提权攻击面）：**

```
identity.bind(P, K) 有效，当且仅当满足以下之一：
  (a) 由已绑定到 P 的密钥签署          // 本人重新绑定（等价于轮换）
  (b) 由 P 的父母签署，且 P 当前无绑定密钥   // 新生儿 / 未成年人代管
  (c) 通过 identity.recover 路径          // 社交恢复
  (d) 通过 identity.succession 路径且 P 已确认死亡  // 遗嘱继承
```

> **一旦 P 绑定了密钥，父母 MUST NOT 再重新绑定。** 否则父母可以随时
> 夺走成年子女的身份。规则 (b) 的 "P 当前无绑定密钥" 条件是硬约束。

**密钥丢失是必然事件，不是意外。** NFC 卡会丢、会消磁，人会死。
实现 MUST 提供社交恢复的可用 UI，否则该协议在真实家庭中无法使用。

### 5.2 人物类

| 类型 | body | 说明 |
|------|------|------|
| `person.create` | `{person}` | 创建人物节点（`person` 为随机 128-bit id） |
| `person.assert` | `{person, field, value}` | 对人物某属性的声明 |

`person.assert` 的 `field` 取值为封闭集合（抗滥用，见 §10）：
`given_name`, `family_name`, `alias`, `sex`, `birth_date`, `birth_place`,
`death_date`, `death_place`, `occupation`, `education`, `note`

`[待定 T2]` 该集合是否需要扩展机制？开放扩展会削弱形状约束。

### 5.3 关系类

| 类型 | body | 说明 |
|------|------|------|
| `union.create` | `{union, partners:[person,person], date?, kind}` | 婚姻/结合 |
| `union.dissolve` | `{union, date?, reason?}` | 离婚/解除 |
| `relation.parent` | `{child, parent, kind, union?}` | 亲子声明 |

`relation.parent.kind` ∈ `{biological, adoptive, step, foster}`
`union.kind` ∈ `{marriage, partnership}`

一个人 MAY 有多个 union（再婚）。子女挂在具体的 union 上。

### 5.4 内容类

| 类型 | body | 说明 |
|------|------|------|
| `life.event` | `{person, date, title, text?, place?, media?:[cid]}` | 生平时间轴条目 |
| `bio.write` | `{person, section, text}` | 传记段落 |
| `media.attach` | `{person, date, cid, mime, bytes, thumb, meta}` | 媒体附加 |

**`media.attach` MUST 同时指定 `person` 与 `date`。** 不存在游离媒体（N5）。

### 5.5 死亡类

| 类型 | body | 说明 |
|------|------|------|
| `death.assert` | `{person, date?, place?, endorsements:[...]}` | 标记死亡 |
| `death.revoke` | `{person, assert_id}` | 本人撤销 |

详见 §6.2。

### 5.6 治理类

| 类型 | body | 说明 |
|------|------|------|
| `content.flag` | `{target_id, reason, endorsements:[...]}` | 标记不当内容 |
| `genesis.endorse` | `{root_person, founders:[pk], family_name}` | 创世背书 |

---

## 6. 权限模型（"用状态机实现家庭法"）

这是本协议真正的核心难点。所有规则 MUST 可由事件集合纯函数式推导，
MUST NOT 依赖任何存储的权限表——存储的权限表就是变相的管理员。

### 6.1 基本规则

设 `K` 为签名密钥，`A = person(K)` 为其绑定的人物节点，`P` 为目标人物：

```
canEdit(K, P) :=
     A == P                                          // 1. 本人
  ∨  P ∈ activeSpouses(A)                            // 2. 现任配偶
  ∨  P ∈ descendants(A)                              // 3. 全部子孙
  ∨  (P ∈ ancestors(A) ∧ deathConfirmed(P))          // 4. 已确认死亡的祖先
  ∨  isGuardianOf(K, P)                              // 5. 未成年人监护
  ∨  (isHeirOf(K, P) ∧ deathConfirmed(P))            // 6. 遗嘱继承
```

推论：
- **兄弟姐妹之间无编辑权**（不是彼此的子孙）。由共同父母或本人管理。
- **向上无权限**，除非祖先已确认死亡。这是设计的核心约束。
- 夫妻**对称**互编。

### 6.2 死亡确权的三层防护（最关键的攻击面）

`deathConfirmed(P)` 是系统中**唯一的提权路径**：一个不孝子可以在父亲
还活着时宣告其死亡，进而改写他的一生。三层防护：

**第一层：法定人数（quorum）**

```
Q(P) = activeSpouses(P) ∪ children(P)
若 |Q(P)| < 2:  Q(P) ∪= parents(P) ∪ siblings(P)      // 独生子女情形放宽集合
k = max( min(2, |Q(P)|),  ceil(|Q(P)| / 2) )
```

`death.assert.endorsements` MUST 包含 ≥ k 个来自 `Q(P)` 中**不同成员**
对同一 `{person, date}` 的签名。

**第二层：公示期与本人撤销**

- 公示期为 **30 天** `[待定 T3]`。
- **公示期 MUST 从「本节点首次收到该 `death.assert` 的本地时间」起算，
  MUST NOT 使用事件的 `ts`。** 否则攻击者只需把 `ts` 填成 30 天前，
  死亡即刻生效。
- 公示期内，由绑定到 `P` 的密钥签署的 `death.revoke` 使该声明**永久失效**。
- **`death.revoke` 无时限**：即使已过公示期、已确认死亡，收到撤销后仍
  MUST 撤销确认。理由：人若真死了就签不出撤销；能签出来就证明还活着。
- 被撤销的 `death.assert` MUST 永久保留并展示——它是"某年某月某人试图
  宣告我死亡"的永久记录。

> 后果：不同节点会在不同的墙钟时刻确认死亡。这是可接受的，因为它只影响
> 本地何时解锁编辑权，不影响任何已写入记录的有效性。

**第三层：降低夺权的收益（最重要）**

由 §2.2，**本人生前签署的内容永远不可被覆盖，只能被补充**。因此夺权成功
最多只能往祖先的传记里*增加*内容，删不掉他自己写的一个字，且所有补充都
永久标注"由 X 于某时补充"。

**攻击的收益被压到很低，这比死守"不让夺权"更实际。**

### 6.3 验证的时序问题（重要且反直觉）

事件的有效性是**相对于节点当前持有的事件集合**判定的，而非绝对属性。

收到新事件可能改变旧判定——例如后来才得知某次离婚，则前配偶此后写的
记录追溯性变为越权。因此：

- 有效性是**可修订的**，不是一次性的（这也是 §4.4 存储规则的原因）
- UI MUST 展示"当前判定"，MUST NOT 表述为绝对真相
- 实现 SHOULD 在重新判定导致状态翻转时通知用户

### 6.4 最小证明链

远房亲戚的记录，接收方可能缺少验证权限所需的那段祖先链。

因此同步单元不是裸事件，而是 **bundle**：

```json
{ "events": [ ... ], "proof": [ ... ] }
```

`proof` 携带从共同祖先到作者的 `relation.parent` / `identity.bind` 事件，
供接收方离线完成权限判定。`proof` **不参与事件签名**（否则会改变 id），
仅是传输层的携带物。

---

## 7. 冲突与合并

### 7.1 合并 = 并集

两个节点同步时，合并操作就是**事件集合求并集**。因为事件是内容寻址且
签名的，重复事件自动去重（id 相同），且从任何来源获取都同样安全。

**不存在合并冲突。** 冲突发生在展示层，不在存储层。

### 7.2 展示优先级

同一 `{person, field}` 存在多条声明时，按以下顺序排序：

```
score(claim) = (author 是 person 本人 ? 1000 : 0) - bloodDistance(author, person)
平分时：seq 较大者优先；仍平分时：id 字典序较小者优先（确定性 tie-break）
```

`bloodDistance` = 族谱图上的最短跳数。即：

```
本人签署  >  父母  >  祖父母  >  更远的祖先
```

**确定性 tie-break 是必需的**，否则两个实现会展示不同结果。

### 7.3 冲突 MUST 可见

存在多条声明时，UI MUST 显示"另有 N 条不同记载"的入口，
点开可见每条的作者、时间与内容。

MUST NOT 静默丢弃低优先级声明。

---

## 8. 媒体

### 8.1 白名单

只接受：`image/jpeg`, `image/png`, `image/webp`, `video/mp4`, `audio/mpeg`,
`audio/aac`, `application/pdf` `[待定 T4]`

### 8.2 强制重编码

**所有媒体在入库前 MUST 被重新编码**：

| 输入 | 输出 |
|------|------|
| 图片 | JPEG (q≈85) 或 WebP |
| 视频 | H.264 或 AV1，MP4 容器 |
| 音频 | AAC 或 Opus |

重编码不是为了省空间，而是**摧毁隐写载荷与容器夹带**（§10）。

元数据除 `DateTimeOriginal` 外 MUST 全部剥除，**GPS 信息 MUST 剥除**
（家庭住址泄露风险）。

### 8.3 内容寻址

```
cid = "sha256-" || hex( SHA-256( "familytree/media/v1" || 0x00 || 重编码后的字节 ) )
```

账本中只存 `cid` + 元信息 + 缩略图，**原件不进账本**。这样账本本身
永远维持在 MB 量级，同步很快。

缩略图 MUST ≤ 32 KB，内联于 `media.attach` 事件（base64url）。
原件按需拉取；拉不到时 UI SHOULD 显示缩略图并标注"原件暂时离线"。

### 8.4 尺寸上限

单件：图片 ≤ 20 MB，视频 ≤ 2 GB，PDF ≤ 50 MB `[待定 T5]`

---

## 9. 同步协议

服务器（relay）**只做一件事：广播"有新东西了"**。它不存储族谱、不理解
亲缘关系、不能解密任何内容。它挂掉、跑路或被查封，家族数据一点不少。

### 9.1 频道派生

**路由知识在客户端，因为客户端有账本。** relay MUST NOT 得知亲缘关系
——那是比照片和传记更敏感的数据。

A 与 B 建立亲缘关系时，各自计算出同一个不透明频道号：

```
ss         = X25519( sk_enc(A), pk_enc(B) )          // 双方结果相同
channel_AB = hex( HMAC-SHA256( key = ss,
                               msg = "familytree/channel/v1" ) )
```

relay 看到的只是一个 32 字节随机串。它不知道 `7f3a9c...` 是谁，
不知道是兄弟还是父子，也不知道发生了什么事。

### 9.2 relay 消息（硬上限 1024 字节）

```json
{
  "v": 1,
  "channel": "<64 hex>",
  "head": "<64 hex>",
  "seq": 42,
  "env": "<base64url, ≤512B, XChaCha20-Poly1305 密文>",
  "sig": "<base64url>"
}
```

`env` 解密后的明文：`{"addr": ["https://...", "..."], "hint": "..."}`

**摘要中 MUST NOT 出现任何语义信息**（不得写"A 家添丁了"），只放
头哈希与加密后的地址。等价于 git 的 ref advertisement：B 发现 `head`
与本地对不上，就知道该拉了。

**relay 的行为约束：**

- MUST 拒绝任何大小 > 1024 字节或形状不符的消息
- MUST NOT 解密 `env`，MUST NOT 解析语义
- MAY 按 channel / IP 限流
- 若提供缓存（§9.3），MUST 设单块上限与短 TTL

> **1 KB 上限是一条抗滥用措施，不是性能优化。** 一个只能转发 1 KB 定长
> 结构的服务器，无法被当作隐蔽传输通道使用。

### 9.3 拉取与可用性

家里的机器在 NAT 后面、大部分时间关机。纯 p2p 直连会大量失败。两个互补机制：

**(a) 亲戚互为镜像。** C 已同步过 A 的更新，则 B 可从 C 处拉取。
数据是内容寻址且签名的，从谁那里取都一样安全。
**家族人越多，可用性越高**——这与"家族信息自动汇集"的初衷一致。

**(b) relay 缓存密文。** relay MAY 暂存加密块，密钥只在亲属之间。
它存的是自己读不懂的字节——不是"承诺不看"，是"看不了"。

推荐第一版直接上 (b)，(a) 作为优化。

### 9.4 验证顺序

接收方 MUST 按此顺序处理，任何一步失败即拒绝该事件：

1. 结构与大小校验
2. `sig` 对 `signing_input` 验签
3. `id == SHA-256(signing_input)`
4. 哈希链连续性（`prev` / `seq`）与分叉检测（§4.2）
5. 权限判定（§6），失败时**保留但标记为无效**（§4.4）

---

## 10. 抗滥用约束

任何足够通用的加密数据交换系统都可能被滥用。本协议的立场是：
**抗滥用要靠产品形状，不能靠事后监控**——监控恰好会摧毁 G2。

需要说明的是，本协议对犯罪用途而言是**很差的工具**：身份长期且锚定于
亲缘图，入会需背书，所有记录永久追加签名不可删。
**为保护奶奶传记而设计的"不可篡改"，同时是一份赖不掉的永久证据链。**

按杠杆大小排列的约束（实现 MUST 全部遵守）：

| # | 约束 | 位置 |
|---|------|------|
| A1 | schema 只允许族谱形状的数据，无自由二进制字段、无任意 KV | §5 |
| A2 | 媒体必须挂载 person + date，不存在游离 blob | §5.4 |
| A3 | 所有媒体强制重编码，摧毁隐写与容器夹带 | §8.2 |
| A4 | relay 消息硬上限 1 KB，形状固定 | §9.2 |
| A5 | 无全局搜索、无陌生人可达 | N3 |
| A6 | 无自由文本消息通道 | N4 |
| A7 | 入会需亲缘背书，无自助注册 | §5.1 |

**客户端侧已知哈希比对**：针对 CSAM，实现 SHOULD 在**加密之前于本地**
对媒体做感知哈希比对。局限须如实说明：只能拦已知素材；正规哈希库
（PhotoDNA / NCMEC）需注册主体方可获取；该做法在公民自由领域存在真实争议。

**下架机制复用权限模型，不新造管理员**：`content.flag` 由家族成员按
§6.2 的 quorum 规则共同签发，各节点收到后停止展示与分发。
这不是全局删除（N2 说明了为何做不到），但**家族自己就是自己子树的版主**。

**做不到的事**：开源之后一定有人能改客户端绕过全部客户端侧检查。这对
任何协议都成立。正确的目标不是"杜绝滥用"，而是
**不主动提供一个好用的犯罪工具，并且在知情后不成为帮凶**。

---

## 11. 创世

第一批人的密钥总得有人发。这个引导阶段必然有个类管理员的动作，
但 MUST 设计成一次性的。

```
根人物节点在收到 ≥ k 个来自不同创始人、内容一致的 genesis.endorse 后成立
k = ceil( 2n / 3 )，且 n ≥ 2
```

例：爷爷生了四个儿子，四兄弟各自建节点。爷爷这个根节点由四兄弟
**共同签名创建**（n=4, k=3），任何一份单独的背书都不算数。
这样既没有管理员，又保证四个分支从同一个根长出来。

**创始人身份 MUST NOT 带有任何后续特权。** 创世结束后，§6 的规则完全接管。

---

## 12. 待定问题

| ID | 问题 | 推荐方案 |
|----|------|----------|
| **T1** | 协议正式命名 | 冻结前必须定，会硬编码进域分隔字符串（§0） |
| **T2** | `person.assert.field` 是否需要开放扩展机制 | 倾向**不开放**，开放会削弱形状约束 A1 |
| **T3** | 公示期长度（暂定 30 天） | 30 天。太短则长辈来不及反应，太长则丧事期间无法记录 |
| **T4** | 媒体白名单是否含 PDF | 倾向**含**（族谱扫描件、老证书是真实需求） |
| **T5** | 媒体尺寸上限 | 图片 20MB / 视频 2GB / PDF 50MB |
| **T6** | **是否设"户主"** | 倾向**不设**。一旦指定户主，就要回答"户主死了权限归谁""为什么是父亲不是母亲"，又绕回管理员问题。§6.1 采用夫妻对称方案 |
| **T7** | **离婚后的权限** | 倾向：`union.dissolve` 后互相失去配偶编辑权，但双方对**共同子女**的权限保留（仍是子孙），历史记录全部保留 |
| **T8** | **新生儿密钥移交后，父母是否保留权限** | 倾向**保留**。§6.1 规则 3 天然导出（子女仍是子孙），且本人签署优先级最高（§7.2），实际影响很小 |
| **T9** | 创世法定比例（暂定 2/3） | `ceil(2n/3)`，n≥2 |

> T6 / T7 / T8 是**家庭法的状态机编码**，是本项目真正的核心难点，绕不过去。
> 本文档已按推荐方案写入正文；改动它们需同步修订 §6.1。

---

## 13. 测试向量

`[待补]` 冻结前 MUST 补齐。这是被严重低估的一环。

需要覆盖：

1. **规范化序列化**：含中日韩人名（NFC 边界情形）、含转义字符、
   键序边界情形 → 期望的 JCS 字节串
2. **事件 ID 与签名**：固定密钥 + 固定信封 → 期望的 `id` 与 `sig`
3. **频道派生**：固定双方密钥 → 期望的 `channel`
4. **权限判定**：一组构造的族谱 + 一批事件 → 每条的期望判定结果
5. **展示排序**：冲突声明集合 → 期望的排序结果（验证 tie-break 确定性）
6. **死亡确权**：quorum 计算与公示期状态机的边界情形

**作用**：几十年后有人用某种今天还不存在的语言重写客户端时，
能拿这组向量验证自己实现对了。没有它，跨实现兼容性靠祈祷。

---

## 14. 实现路线

| 阶段 | 内容 |
|------|------|
| M0 | 本文档 + 测试向量冻结 |
| M1 | `core`：规范化序列化、签名、事件、哈希链、权限判定、合并（纯函数，无 I/O） |
| M2 | 本地单节点：族谱图渲染、人物页、时间轴、相册 |
| M3 | 身份与密钥：生成、绑定、社交恢复、继承 |
| M4 | 同步：频道、relay、bundle 拉取、冲突展示 |
| M5 | 可复现构建 + 第二个独立实现 |

> **M5 不是可选项。** §6 的权限规则由客户端代码执行——谁控制客户端，
> 谁就控制"家庭法"。协议层没有管理员而代码层有一个，等于把管理员
> 从前门赶出去、从后门放进来。**可复现构建才是 G1 真正落地的地方。**

---

## 附录 A：本文档语言

本草案以中文书写，因为当前唯一读者是作者本人。

**在协议冻结与公开发布前，MUST 提供英文版本**，且英文版为规范性文本。
理由见 §13：本协议的存续依赖于独立实现的出现，而独立实现依赖于
规范能被本项目语言圈之外的人读懂。
