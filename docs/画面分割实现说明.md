# main.cpp 火灾检测流水线实现说明

## 概述

本程序实现了一个基于 YOLOv8-seg 的火灾/烟雾检测流水线，包含两个核心功能：

1. **画面分割 (ROI 提取)** — 通过实例分割模型识别视频帧中的火焰和烟雾区域，将非目标区域遮罩为黑色
2. **时序分类 (ConvLSTM)** — 对连续帧序列进行时序分析，区分动态火焰、静态误报和无检测状态

---

## 整体架构流程图

```mermaid
flowchart TB
    subgraph Init["初始化阶段"]
        A[解析命令行参数] --> B[初始化 CUDA]
        B --> C[加载 YOLOv8-seg 引擎]
        C --> D[创建执行上下文]
        D --> E[分配 GPU 张量]
        E --> F[打开视频文件]
        F --> G[创建 SegmentDetector]
        G --> H[创建 ROIPipeline]
        H --> I{启用时序分类?}
        I -->|是| J[加载 ConvLSTM 引擎]
        J --> K[创建 ConvLSTMClassifier]
        I -->|否| L[进入主循环]
        K --> L
    end

    subgraph Process["帧处理循环"]
        L --> M[读取视频帧]
        M --> N[ROI 流水线处理]
        N --> O[预处理 Letterbox]
        O --> P[YOLOv8-seg 推理]
        P --> Q[后处理 NMS + 掩膜生成]
        Q --> R[ROI 提取 + EMA 平滑]
        R --> S[应用掩膜到原始帧]
        S --> T{时序分类启用?}
        T -->|是| U[添加帧到滑动窗口]
        U --> V{缓冲区已满?}
        V -->|是| W[ConvLSTM 推理]
        W --> X[显示分类结果]
        V -->|否| X
        T -->|否| X
        X --> Y[添加叠加信息]
        Y --> Z[显示处理后的帧]
        Z --> AA{继续?}
        AA -->|是| M
        AA -->|否| BB[清理资源]
    end

    Init --> Process
```

---

## 命令行参数

```bash
Usage: fire_detection_test --video <path> --engine <path> [options]

Required arguments:
  --video PATH        输入视频文件路径
  --engine PATH       YOLOv8-seg TensorRT 引擎路径

Options:
  --confidence FLOAT  检测置信度阈值 (默认: 0.5)
  --iou FLOAT         NMS IoU 阈值 (默认: 0.45)
  --no-display        禁用显示 (无头模式)
  --help              显示帮助信息

Temporal Classification (ConvLSTM):
  --convlstm-engine PATH  ConvLSTM 引擎路径 (启用时序分类)
  --seq-len INT           ConvLSTM 序列长度 (默认: 10)
```

---

## 核心组件详解

### 1. 预处理流程 (`preprocessFrame`)

```mermaid
flowchart LR
    subgraph Input["输入"]
        A[原始帧 BGR<br>任意尺寸]
    end

    subgraph Letterbox["Letterbox 变换"]
        B[计算缩放比例<br>scale = min W/w, H/h] --> C[缩放图像<br>保持宽高比]
        C --> D[添加灰色边框<br>114, 114, 114]
    end

    subgraph Convert["格式转换"]
        E[BGR → RGB] --> F[归一化 0-1<br>/255.0]
        F --> G[HWC → CHW<br>通道优先]
    end

    subgraph Output["输出"]
        H[input_tensor<br>1×3×640×640]
    end

    Input --> Letterbox --> Convert --> Output
```

**关键参数:**
- 目标尺寸: 640 × 640
- Letterbox 填充颜色: (114, 114, 114) 灰色
- 返回值: `(scale, pad_x, pad_y)` 用于后续坐标还原

**代码位置:** [main.cpp:182-230](src/main.cpp#L182-L230)

### 2. YOLOv8-seg 推理流程

```mermaid
flowchart TB
    subgraph Model["YOLOv8-seg 模型"]
        A[input_tensor<br>1×3×640×640]
        B[TensorRT Engine<br>FP16/INT8 加速]
        C[output0<br>1×116×8400<br>检测结果]
        D[output1<br>1×32×160×160<br>掩膜原型]
    end

    A --> B
    B --> C
    B --> D

    subgraph Decode["output0 解码 (每个检测)"]
        E["[0:4] x, y, w, h<br>边界框中心+尺寸"]
        F["[4:7] class scores<br>3个类别置信度"]
        G["[7:39] mask coeffs<br>32个掩膜系数"]
    end

    C --> E
    C --> F
    C --> G
```

**输出张量说明:**

| 输出 | 形状 | 说明 |
|------|------|------|
| output0 | (1, 116, 8400) | 8400 个候选框，每个 116 维 = 4(bbox) + 3(类别) + 32(掩膜系数) + 77(保留) |
| output1 | (1, 32, 160, 160) | 32 个 160×160 的掩膜原型基 |

**代码位置:** [main.cpp:364-383](src/main.cpp#L364-L383)

### 3. 后处理流程 (`YOLOv8PostProcessor`)

```mermaid
flowchart TB
    subgraph PostProcess["后处理流程"]
        A[output0 + output1] --> B[解码检测框<br>xywh → xyxy]
        B --> C[置信度过滤<br>conf > threshold]
        C --> D[NMS 非极大值抑制<br>IoU > iou_threshold]
        D --> E[生成实例掩膜]
    end

    subgraph MaskGen["掩膜生成"]
        F["mask_coeffs<br>32 维向量 (c₁...c₃₂)"] --> H["矩阵乘法<br>Σ cᵢ × Pᵢ"]
        G["prototypes P<br>32×160×160"] --> H
        H --> I["Sigmoid 激活<br>σ(x) = 1/(1+e⁻ˣ)"]
        I --> J[缩放到原始尺寸]
        J --> K["阈值化 > 0.5<br>得到二值掩膜"]
    end

    E --> MaskGen
```

**掩膜生成公式:**
```
mask_160x160 = sigmoid(Σᵢ mask_coeffsᵢ × prototypesᵢ)
mask_binary = resize(mask_160x160, original_size) > 0.5
```

**代码位置:** [yolov8_postprocess.cpp](src/yolov8_postprocess.cpp)

### 4. SegmentDetector 检测器类

```mermaid
flowchart TB
    subgraph SegmentDetector["SegmentDetector::detect()"]
        A[输入帧 frame] --> B[preprocessFrame<br>Letterbox + 归一化]
        B --> C[engine_.infer<br>TensorRT 推理]
        C --> D[copyToVector<br>GPU → CPU]
        D --> E[postprocessor_.process<br>NMS + 掩膜生成]
        E --> F[Detection → ROIDetection<br>格式转换]
        F --> G[输出 ROIDetection 列表]
    end
```

**代码位置:** [main.cpp:235-305](src/main.cpp#L235-L305)

### 5. ROI 提取流水线 (`ROIPipeline`)

```mermaid
flowchart TB
    subgraph Detection["检测分类"]
        A[所有检测结果] --> B{按类别分类}
        B -->|class_id=0| C[火焰检测]
        B -->|class_id=2| D[烟雾检测]
        B -->|class_id=1| E[人员<br>忽略]
    end

    subgraph FireProcess["火焰处理流程"]
        C --> F[使用分割掩膜<br>精确边缘]
        F --> G[EMA 时序平滑<br>减少抖动]
        G --> H[形态学边缘平滑<br>闭运算 + 模糊]
    end

    subgraph SmokeProcess["烟雾处理流程"]
        D --> I[使用边界框<br>覆盖扩散区域]
        I --> J[批次内 BBox 合并<br>Union 操作]
        J --> K[添加 5% 边距<br>安全区域]
    end

    subgraph Merge["掩膜合并"]
        H --> L[fire_mask]
        K --> M[smoke_mask]
        L --> N["OR 合并<br>final = fire | smoke"]
        M --> N
        N --> O[final_mask]
    end

    subgraph Apply["应用掩膜"]
        O --> P["原始帧 × final_mask<br>逐像素相乘"]
        P --> Q[非 ROI 区域置黑]
    end
```

**处理策略差异:**

| 类别 | class_id | 策略 | 原因 |
|------|----------|------|------|
| 火焰 | 0 | 分割掩膜 + EMA 平滑 | 火焰边缘不规则，需要精确分割和时序稳定 |
| 烟雾 | 2 | 边界框合并 | 烟雾扩散范围大，边界模糊，bbox 更稳定 |
| 人员 | 1 | 忽略 | 负样本类，不参与 ROI 提取 |

**代码位置:** [roi_extractor.cpp](src/roi_extractor.cpp)

### 6. EMA 掩膜平滑 (`MaskEMA`)

```mermaid
flowchart LR
    subgraph EMA["指数移动平均"]
        A["当前帧掩膜<br>Mₜ"] --> C["加权融合<br>EMAₜ = α×Mₜ + (1-α)×EMAₜ₋₁"]
        B["历史 EMA 掩膜<br>EMAₜ₋₁"] --> C
        C --> D[新 EMA 掩膜]
    end

    subgraph Smooth["边缘平滑"]
        D --> E["形态学闭运算<br>消除小孔洞"]
        E --> F["高斯模糊<br>平滑边缘"]
        F --> G["阈值化<br>> 0.5"]
    end

    subgraph Output["输出"]
        G --> H[平滑后的掩膜]
    end
```

**EMA 公式:**
```
EMA_mask_t = α × current_mask_t + (1-α) × EMA_mask_(t-1)
```

**参数影响:**
- α = 0.2 (默认值)
- 较小的 α → 更平滑，但延迟更大
- 较大的 α → 响应更快，但抖动更多

**代码位置:** [roi_extractor.h](include/roi_extractor.h) MaskEMA 类

---

## ConvLSTM 时序分类

### 7. ConvLSTM 分类器 (`ConvLSTMClassifier`)

```mermaid
flowchart TB
    subgraph Buffer["滑动窗口缓冲区"]
        A[ROI 处理后的帧] --> B["预处理<br>640×640, RGB, [0,1]"]
        B --> C["添加到 deque<br>保持最近 N 帧"]
        C --> D{缓冲区已满?}
    end

    subgraph Infer["ConvLSTM 推理"]
        D -->|是| E["组装输入张量<br>(1, N, 3, 640, 640)"]
        E --> F[TensorRT 推理]
        F --> G["输出热力图<br>(1, 3, 20, 20)"]
    end

    subgraph Parse["输出解析"]
        G --> H["Max Pooling<br>每类取空间最大值"]
        H --> I["Softmax<br>归一化概率"]
        I --> J["Argmax<br>取最大概率类别"]
        J --> K[分类结果]
    end

    D -->|否| L[等待更多帧]
```

**分类类别:**

| 类别 | 枚举值 | 说明 | 显示颜色 |
|------|--------|------|----------|
| STATIC | 0 | 静态火焰/误报（如红色物体、灯光） | 橙色 |
| DYNAMIC | 1 | 动态火焰（真实火灾，具有闪烁、扩散特征） | 红色 |
| NEGATIVE | 2 | 无检测（无火焰/烟雾） | 绿色 |

**模型输入输出:**
- 输入: `(1, seq_len, 3, 640, 640)` — seq_len 帧 RGB 图像序列
- 输出: `(1, 3, 20, 20)` — 3 个类别的 20×20 热力图

**代码位置:** [convlstm_classifier.cpp](src/convlstm_classifier.cpp)

### 8. ConvLSTM 预处理流程

```mermaid
flowchart LR
    subgraph Input["输入"]
        A[ROI 帧<br>BGR, 任意尺寸]
    end

    subgraph Preprocess["预处理"]
        B["Letterbox Resize<br>640×640"] --> C["BGR → RGB<br>通道转换"]
        C --> D["归一化<br>[0, 255] → [0, 1]"]
        D --> E["HWC → CHW<br>通道优先"]
    end

    subgraph Buffer["缓冲区"]
        E --> F["添加到 deque<br>frame_buffer_"]
        F --> G["维护窗口大小<br>pop_front if > seq_len"]
    end

    Input --> Preprocess --> Buffer
```

**代码位置:** [convlstm_classifier.cpp:72-117](src/convlstm_classifier.cpp#L72-L117)

---

## 完整数据流向图

```mermaid
flowchart TB
    subgraph Input["输入"]
        V[视频文件]
    end

    subgraph FrameLoop["帧处理循环"]
        V --> F1[读取帧 N]

        subgraph YOLOSeg["YOLOv8-seg 分割"]
            F1 --> PP[Letterbox 预处理]
            PP --> TRT1[TensorRT 推理]
            TRT1 --> O0[output0: 检测结果]
            TRT1 --> O1[output1: 掩膜原型]
        end

        subgraph PostProc["后处理"]
            O0 --> NMS[NMS 过滤]
            O1 --> MG[掩膜生成]
            NMS --> MG
            MG --> DET[Detection 列表]
        end

        subgraph ROIExtract["ROI 提取"]
            DET --> FD[火焰检测]
            DET --> SD[烟雾检测]
            FD --> FM["fire_mask<br>+ EMA 平滑"]
            SD --> SM["smoke_mask<br>BBox 合并"]
            FM --> MERGE[OR 合并]
            SM --> MERGE
            MERGE --> APPLY[应用掩膜]
            F1 --> APPLY
            APPLY --> ROI_FRAME["ROI 帧<br>非目标区域为黑"]
        end

        subgraph Temporal["时序分类 (可选)"]
            ROI_FRAME --> BUF["滑动窗口<br>deque"]
            BUF --> |"缓冲满"| ASM["组装张量<br>(1, N, 3, 640, 640)"]
            ASM --> TRT2[ConvLSTM<br>TensorRT 推理]
            TRT2 --> PARSE["解析输出<br>Max Pool + Softmax"]
            PARSE --> CLS["分类结果<br>DYNAMIC/STATIC/NEGATIVE"]
        end

        subgraph Display["显示输出"]
            ROI_FRAME --> OVERLAY[添加叠加信息]
            CLS --> OVERLAY
            OVERLAY --> SHOW[cv::imshow]
        end
    end
```

---

## 关键常量配置

```cpp
// ===== YOLOv8-seg 模型常量 =====
constexpr int INPUT_WIDTH = 640;          // 模型输入宽度
constexpr int INPUT_HEIGHT = 640;         // 模型输入高度
constexpr int NUM_CLASSES = 3;            // 类别数: 火焰(0)、人员(1)、烟雾(2)
constexpr int MASK_PROTO_H = 160;         // 掩膜原型高度
constexpr int MASK_PROTO_W = 160;         // 掩膜原型宽度
constexpr int NUM_MASK_COEFFS = 32;       // 掩膜系数数量

// ===== ROI 提取参数 =====
constexpr float EMA_ALPHA = 0.2f;         // EMA 平滑因子
constexpr float BBOX_PADDING_RATIO = 0.05f; // 烟雾 bbox 边距 (5%)
constexpr float DEFAULT_CONF_THRESHOLD = 0.5f;  // 默认置信度阈值
constexpr float DEFAULT_IOU_THRESHOLD = 0.45f;  // 默认 NMS IoU 阈值

// ===== ConvLSTM 模型常量 =====
constexpr int CONVLSTM_INPUT_WIDTH = 640;   // ConvLSTM 输入宽度
constexpr int CONVLSTM_INPUT_HEIGHT = 640;  // ConvLSTM 输入高度
constexpr int CONVLSTM_NUM_CLASSES = 3;     // 分类数: STATIC(0), DYNAMIC(1), NEGATIVE(2)
constexpr int CONVLSTM_HEATMAP_SIZE = 20;   // 输出热力图尺寸
constexpr int DEFAULT_SEQ_LEN = 10;         // 默认序列长度
```

---

## 类图

```mermaid
classDiagram
    class Args {
        +string video_path
        +string engine_path
        +string convlstm_engine_path
        +float confidence_threshold
        +float iou_threshold
        +int convlstm_seq_len
        +bool display_enabled
        +bool enable_temporal
        +bool help_requested
        +bool parse_error
        +string error_message
    }

    class SegmentDetector {
        -TrtEngineMultiTs& engine_
        -Tensor~float~& input_tensor_
        -Tensor~float~& output0_tensor_
        -Tensor~float~& output1_tensor_
        -YOLOv8PostProcessor postprocessor_
        +detect(frame, conf_threshold) ROIDetection[]
    }

    class YOLOv8PostProcessor {
        -int num_classes_
        -float conf_threshold_
        -float iou_threshold_
        +process(output0, output1, ...) Detection[]
        +setConfThreshold(threshold)
        -decodeOutput(...) vector~BBox~
        -nms(boxes) vector~BBox~
        -generateMask(...) vector~float~
    }

    class ROIPipeline {
        -DetectionCallback detector_
        -float confidence_threshold_
        -bool use_ema_
        -float ema_alpha_
        -float bbox_padding_
        -MaskEMA fire_ema_
        +processBatch(frames, mode, skip) BatchResult
        -buildFireMask(...) Mat
        -buildSmokeMask(...) Mat
        -applyMaskToFrame(...) tuple
    }

    class MaskEMA {
        -float alpha_
        -int blur_kernel_
        -Mat ema_mask_
        +update(current_mask) Mat
        +reset()
        -smoothEdges(mask) Mat
    }

    class ConvLSTMClassifier {
        -TrtEngineMultiTs& engine_
        -int seq_len_
        -bool initialized_
        -deque~Mat~ frame_buffer_
        -unique_ptr~Tensor~ input_tensor_
        -unique_ptr~Tensor~ output_tensor_
        -InferenceResult last_result_
        +initialize() bool
        +addFrame(roi_frame)
        +infer() bool
        +isBufferFull() bool
        +getClassification() Classification
        +getConfidence() float
        +reset()
        -preprocessFrame(frame) Mat
        -assembleInputTensor()
        -parseOutput()
    }

    class InferenceResult {
        +Classification classification
        +float confidence
        +float class_scores[3]
        +Mat heatmap
    }

    class Classification {
        <<enumeration>>
        STATIC = 0
        DYNAMIC = 1
        NEGATIVE = 2
    }

    SegmentDetector --> YOLOv8PostProcessor : uses
    SegmentDetector --> TrtEngineMultiTs : uses
    ROIPipeline --> MaskEMA : contains
    ROIPipeline ..> SegmentDetector : callback
    ConvLSTMClassifier --> TrtEngineMultiTs : uses
    ConvLSTMClassifier --> InferenceResult : produces
    InferenceResult --> Classification : contains
```

---

## 处理时序图

```mermaid
sequenceDiagram
    participant Main as main()
    participant ROI as ROIPipeline
    participant Det as SegmentDetector
    participant TRT1 as YOLOv8 Engine
    participant Post as YOLOv8PostProcessor
    participant EMA as MaskEMA
    participant Conv as ConvLSTMClassifier
    participant TRT2 as ConvLSTM Engine

    loop 每帧处理
        Main->>ROI: processBatch([frame])
        ROI->>Det: detect(frame, conf)
        Det->>Det: preprocessFrame()
        Det->>TRT1: infer(input, outputs)
        TRT1-->>Det: output0, output1
        Det->>Post: process(output0, output1)
        Post->>Post: decodeOutput()
        Post->>Post: NMS()
        Post->>Post: generateMask()
        Post-->>Det: detections[]
        Det-->>ROI: ROIDetection[]

        ROI->>ROI: filterDetections(fire/smoke)
        ROI->>EMA: update(fire_mask)
        EMA-->>ROI: smoothed_mask
        ROI->>ROI: buildSmokeMask(union_bbox)
        ROI->>ROI: applyMaskToFrame()
        ROI-->>Main: BatchResult (ROI 帧)

        opt 时序分类启用
            Main->>Conv: addFrame(roi_frame)
            Conv->>Conv: preprocessFrame()
            Conv->>Conv: frame_buffer_.push_back()

            alt 缓冲区已满
                Main->>Conv: infer()
                Conv->>Conv: assembleInputTensor()
                Conv->>TRT2: infer(input, output)
                TRT2-->>Conv: heatmap (1, 3, 20, 20)
                Conv->>Conv: parseOutput()
                Conv-->>Main: Classification + Confidence
            end
        end

        Main->>Main: 添加叠加信息
        Main->>Main: cv::imshow()
    end
```

---

## 显示叠加信息

程序在输出帧上显示以下信息：

| 位置 | 内容 | 说明 |
|------|------|------|
| (10, 30) | `Frame: N/Total` | 当前帧/总帧数 |
| (10, 60) | `Inference: X ms` | 单帧推理耗时 |
| (10, 90) | `Status: FIRE/SMOKE/No detection` | 检测状态 |
| (10, 120) | `Buffer: N/SeqLen` | ConvLSTM 缓冲区状态 (仅时序分类启用时) |
| (10, 150) | `Temporal: DYNAMIC/STATIC/NEGATIVE (XX%)` | 时序分类结果 (仅时序分类启用时) |
| (center, 50) | `!!! FIRE ALERT !!!` | 动态火焰警告 (红色) |

---

## 键盘控制

| 按键 | 功能 |
|------|------|
| `q` / `ESC` | 退出程序 |
| `p` | 暂停/继续播放 |
| `Space` | 暂停时单步前进一帧 |

---

## 总结

main.cpp 的火灾检测流水线采用了以下技术栈：

1. **深度学习推理**: TensorRT 加速的 YOLOv8-seg 和 ConvLSTM 模型
2. **实例分割**: 基于 32 维掩膜系数和 160×160 原型的掩膜生成
3. **时序平滑**: EMA 指数移动平均减少掩膜抖动
4. **差异化处理**: 火焰用精确分割 + EMA，烟雾用边界框合并
5. **时序分类**: ConvLSTM 滑动窗口推理，区分动态/静态/无检测
6. **实时可视化**: OpenCV 窗口显示处理结果和分类状态

整个流水线实现了从视频输入到 ROI 提取 + 时序分类输出的端到端处理，适用于实时火灾检测和误报过滤场景。
